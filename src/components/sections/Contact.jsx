import RevealOnScroll from "../RevealOnScroll";

const Contact = () => {

    return (
        <section id="contact">
            <RevealOnScroll>
                

            </RevealOnScroll>
        </section>
    )
}

export default Contact;